- defaults:
    link:
        relink: true

- clean: ['~']

- link:
#     ~/.tmux.conf: tmux.conf
#     ~/.vim: vim
    ~/.zshenv:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.Brewfile:

- create:
    - ~/Documents/Workspace

- shell:
    # - [git submodule update --init --recursive, Installing submodules]
    - echo "hey you guys"
    - brew bundle install --global